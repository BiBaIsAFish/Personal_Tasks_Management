# 1. 預設行為：讓 Git 自動處理所有文字檔
# Git 會在 Commit 時將所有文字檔轉成 LF，Checkout 時視作業系統轉換（但在現代開發中，通常建議搭配後面的規則強制指定）
* text=auto

# 2. 強制指定特定檔案永遠使用 LF (Line Feed)
# 現代網頁開發、腳本、文件（包含你的 md 檔）強烈建議統一使用 LF
*.md text eol=lf
*.txt text eol=lf
*.js text eol=lf
*.json text eol=lf
*.html text eol=lf
*.css text eol=lf
*.py text eol=lf
*.sh text eol=lf

# 3. 強制指定特定檔案永遠使用 CRLF (Carriage Return + Line Feed)
# 只有在 Windows 專屬腳本或特定依賴 CRLF 的專案才需要
*.bat text eol=crlf
*.cmd text eol=crlf

# 4. 標記二進位檔案 (Binary)
# 告訴 Git 這些檔案不是純文字，絕對不要去動它們的換行字元
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
*.woff binary
*.woff2 binary